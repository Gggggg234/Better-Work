; Use the operating-system trust store for TLS so Node's fetch can verify
; certificates issued by a local/corporate/antivirus root CA (otherwise
; server-side Supabase calls fail with UNABLE_TO_VERIFY_LEAF_SIGNATURE).
; Requires Node >= 22.
node-options=--use-system-ca
